# 2026-09-26 工作日志

## Owlsgo-Chat v1.0.0 从零创建完成并推送 GitHub

- 按用户要求先读设计文档（开发文档/色彩搭配/安全审核提示词/图床 API 文档），用 birdview 生成架构图（`.birdview/architecture.json`，rev 2，13 模块 18 关系 9 约束，校验通过）。
- 关键裁决：Workerman WebSocket + Redis 限流 与「零 Composer 依赖」硬性规则冲突 → 用户选择 **AJAX 长轮询 + 数据库计数限流**。
- 交付：纯原生 PHP 8.1+，根目录 index.php 单入口，core/ 九模块（config/db/security/mail/auth/plugin/chat/upload/admin），assets/ 前端（ow 前缀扁平 UI、ES5 XHR、自研线性 SVG 图标、响应式），Logo 用设计文档/index.svg。
- 冒烟测试（/d/tools/php83/php.exe 8.3.33，需 -d 手动加载 mbstring/pdo_sqlite/curl 等扩展）：安装向导、签名登录、发言/历史/私信/撤回/长轮询即时返回、上传、SVG 验证码、游客、禁言/解禁、敏感词、后台全部通过。
- 重要修复：长轮询动作必须 `session_write_close()`，否则 PHP 会话文件锁会阻塞同会话其他请求（PHP-FPM 生产同样存在）；Windows 下 PHP_CLI_SERVER_WORKERS 无效，内置服务器单进程，本地无法测真并发推送，属环境限制。
- 版本号：VERSION 文件 + index.php 常量 OWLSGO_VERSION = 1.0.0。
- GitHub：zealis/Owlsgo-Chat（公开仓库，与旧项目 owlsgo/owlsgo-v3 同账号）。本机无 gh CLI，但 GCM 已存凭据，`GCM_INTERACTIVE=never git push` 可直接推送。git 身份 zealisnex/zealisnex@outlook.com（仓库级配置）。
- 已推送：commit e913bc0（v1.0.0 首版），基于远程 Initial commit (LICENSE)。
